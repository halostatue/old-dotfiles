[extdiff]
cmd.ksdiff = /usr/local/bin/ksdiff
opts.ksdiff = --changeset --wait --filelist
cmd.chdiff = /usr/bin/chdiff
opts.chdiff = --wait
cmd.gvimdiff = mvim

[ui]
merge = Kaleidoscope

[merge-tools]
changes.executable = chdiff
changes.args = --wait $local $other
Kaleidoscope.executable = /usr/local/bin/ksdiff
Kaleidoscope.args = --merge --output $output --base $base -- $local $other

[core]
editor = mvim --remote-wait-silent

[web]
<%= when_exists('/etc/hg-dummy-cert.pem') { "cacerts = /etc/hg-dummy-cert.pem" } %>
#<%= when_exists('/etc/hg-ca-roots.pem') { "cacerts = /etc/hg-ca-roots.pem" } %>

# vim: syntax=cfg
