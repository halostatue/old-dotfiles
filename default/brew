tap homebrew/dupes
tap homebrew/versions
tap phinze/cask

# GNU core utilities (OS X versions are outdated)
# install coreutils

# GNU find, locate, updatedb, xargs; g-prefixed (e.g., gxargs).
# install findutils

# Bash4
# install bash

# Zsh5
# install zsh

# Note that the order in which these are installed is important; see
# http://git.io/brew-narwhal-ringo.
install ringojs
install narwhal

# Install more recent versions of some OS X tools
# install homebrew/dupes/grep
# tap josegonzalez/homebrew-php
# install php54

install git
install node
install tree
install webkit2png
install wget --enable-iri
